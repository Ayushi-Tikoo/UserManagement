import React, { Fragment } from 'react';
import { WaveTopBottomLoading } from 'react-loadingg';

const Loader = () => (
  <Fragment>
    <WaveTopBottomLoading color='black' size={'large'} />
  </Fragment>
);

export default Loader;
